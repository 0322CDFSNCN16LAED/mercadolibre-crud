const controller = {
    index: (req, res) => {
        throw new Error("Do the magic!");
    },
    search: (req, res) => {
        throw new Error("Do the magic!");
    },
};

module.exports = controller;
